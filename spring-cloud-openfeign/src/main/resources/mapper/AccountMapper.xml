<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maguasoft.example.openfeign.mapper.AccountMapper">
    <select id="getAccount" resultType="com.maguasoft.example.openfeign.domain.Account">
        SELECT * FROM account LIMIT 1;
    </select>
</mapper>